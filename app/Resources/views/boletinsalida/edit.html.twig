{% extends 'base.html.twig' %}

{% block content %}
    <h1 align="center">Editar Boletin de Salida</h1>
    <div style="width:100%;padding:10px 10px 10px 10px">
        {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <input type="submit" value="Edit" class="btn btn-success btn-sm"/>
        {{ form_end(edit_form) }}
        <p>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger btn-sm">
            {{ form_end(delete_form) }}
        </p>
    </div>
    <hr></hr>
    <div align="center">
        <p>
            <a class="btn btn-primary btn-sm" href="{{ path('boletinsalida_index') }}">Back to the list</a>
        </p>
    </div>
{% endblock %}
